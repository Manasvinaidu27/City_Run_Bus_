services:
  - type: web
    name: tsrtc-smartticket
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT
    plan: free